// Funciones flecha
// Estas funciones fueron introducidas desde la versión 6 de ECMA

// Declaración de una función flecha
// Es más recomendable usar const para las funciones flechas
// No es necesario usar la palabra reservada function
const sumar = (a, b) => a+b;

console.log(sumar(1, 2));
console.log(sumar(3, 4));
console.log(sumar(5, 6));
