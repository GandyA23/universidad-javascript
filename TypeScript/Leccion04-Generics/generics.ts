// Declaración de un arreglo de números
let arregloNumeros: Array<number>;

arregloNumeros = [1, 2, 3, 4, 5];

// Generá un error, debido a que el arreglo no acepta strings
// arregloNumeros = ["Gandy", "Esaú"];

// Imprime todo el arreglo
console.log(arregloNumeros);

// Imprime una posición
console.log(arregloNumeros[0]);
