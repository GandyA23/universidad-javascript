// Identifica sí un número es par o no

let a1 = 2;
let a2 = 1;

// Cuando es una sola línea, es posible dejarlo sin llaves
// ya que, solo tomara la siguiente línea para ejecutar
if (a1 % 2)
    console.log("El número", a1, "es impar");
else
    console.log("El número", a1, "es par");

if (a2 % 2)
    console.log("El número", a2, "es impar");
else
    console.log("El número", a2, "es par");
